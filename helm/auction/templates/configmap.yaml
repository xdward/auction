apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "auction.fullname" . }}-endpoints
data:
  NATS_ADDRESS: {{ include "auction.natsAddress" . | quote }}
  REDIS_ADDRESS: {{ include "auction.redisAddress" . | quote }}
